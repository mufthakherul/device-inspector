# Inspecta Plugin SDK (Rust)

This is a Rust adapter-track skeleton for capability-safe plugin development.

## Included
- `Cargo.toml` package scaffold.
- `src/lib.rs` contract placeholder for deterministic plugin responses.

The runtime negotiates capabilities before plugin execution.
