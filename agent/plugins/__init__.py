# Copyright (c) 2025 mufthakherul — see LICENSE.txt
"""Plugins for external tool integration (smartctl, memtester, fio).

This package contains lightweight wrappers and parsers. In CI we use the
parsers against fixture JSON rather than invoking native binaries.
"""
